require "spec_helper"

RSpec.describe "Task Creation and Assignment", type: :feature do
  before(:all) do
    @tasker
  end

  describe "Creating task with valid details" do
    scenario "when creating a Task with location" do

    end
  end
end
