deployment:
  staging:
    branch: master
    heroku:
      appname: workdey-staging
machine:
  ruby:
    version: 2.2.3
# database:
#   override:
#     - mv config/database.ci.yml config/database.yml
#     - bundle exec rake db:create db:migrate db:seed permissions:permissions --trace